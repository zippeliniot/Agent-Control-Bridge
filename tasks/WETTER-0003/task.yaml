schema_version: '1.0'
kind: bridge_task
bridge_task_id: WETTER-0003
project_id: wetter-app
title: 'Kopfzeilen-Konsistenz: aktuelle Werte in Temperatur/Wind analog Taschensee/Ostsee'
description: 'Teil A: index.html #temp-card .chart-head um Span #temp-current ergaenzen;
  js/main.js Funktion updateTempHeader() (Referenzstadt via getWindCityIdx(), Temperatur
  + Niederschlag aus cachedData[idx].current), Aufruf an allen applyLocationFilter()-Stellen
  UND in beiden Zweigen von loadAll(). Teil B: index.html #wind-card .chart-head um
  Span #wind-current ergaenzen; js/main.js Funktion updateWindHeader() (immer Gronenberg
  index 1, Windgeschwindigkeit + -richtung via getWindDir()), gleiche Aufrufstellen
  wie Teil A. Teil C: #sea-current nur verifizieren (kein Codeaendern), Befund im
  Bericht.'
task_class: FEATURE
repository: wetter-app
branch: main
git:
  expected_head: 4831a8e5bcfaf599a577261395fa6240da0b1691
  allowed_changed_files:
  - index.html
  - js/main.js
permissions:
- WORKTREE_WRITE
- TEST_EXECUTION
- GIT_PUSH
model: Claude Sonnet 5
reasoning_level: MEDIUM
executor: claude-code
controller: human
status: REVIEW_REQUIRED
created_at: '2026-09-23T12:00:00Z'
created_by: claude-code
depends_on:
- WETTER-0002
stop_conditions:
- SCOPE_VIOLATION
- CONCEPT_CONFLICT
acceptance_criteria:
- Teil A umgesetzt (temp-current, Erstladen + Standortwechsel funktionieren), im Ziel-Repo
  committet und gepusht
- Teil B umgesetzt (wind-current, Erstladen funktioniert, immer Gronenberg), im Ziel-Repo
  committet und gepusht
- Teil C verifiziert (Ostsee-Kopf funktioniert weiterhin nach Cache-Leerung) - kein
  Codeaendern falls OK
- Manuelle Pruefung ohne Konsolenfehler bestanden
- Scope eingehalten (nur index.html, js/main.js im Ziel-Repo)
task_version: 6
